language: generic 
sudo: required
services:
  - docker

before_install:
  - docker build -t marcelolandivar/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true marcelolandivar/docker-react npm run test